---
phase: 02-visuals
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - css/style.css
  - js/gallery.js
  - images/photos/.gitkeep
autonomous: false

must_haves:
  truths:
    - "Photos appear as polaroid-style cards with white border and caption"
    - "Photos are scattered at varied angles like tossed on a table"
    - "Photos reveal sequentially after click-to-start (dealt animation)"
    - "Clicking a photo opens it in a lightbox overlay"
    - "Clicking outside lightbox or pressing Escape closes it"
    - "Hover on polaroid shows subtle lift effect"
  artifacts:
    - path: "js/gallery.js"
      provides: "Gallery initialization and lightbox interaction"
      min_lines: 30
    - path: "css/style.css"
      provides: "Polaroid styling, scatter rotations, lightbox dialog"
      contains: ".polaroid"
    - path: "index.html"
      provides: "Gallery section with polaroid figures and dialog element"
      contains: "gallery"
  key_links:
    - from: "js/gallery.js"
      to: "app:started"
      via: "event listener"
      pattern: "app:started"
    - from: "js/gallery.js"
      to: "dialog.lightbox"
      via: "showModal()"
      pattern: "showModal"
    - from: ".polaroid"
      to: "lightbox"
      via: "click handler"
      pattern: "addEventListener.*click"
---

<objective>
Implement polaroid-style photo gallery with scattered layout, staggered reveal animation, and native dialog lightbox.

Purpose: Display couple's photos in a romantic, personal way that feels like photos tossed on a table. Clicking enlarges for viewing.
Output: Gallery of 5-7 polaroid-style photos with handwritten captions, clickable lightbox, and "dealt" reveal animation after start.
</objective>

<execution_context>
@C:\Users\bkauer\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\bkauer\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visuals/02-CONTEXT.md
@.planning/phases/02-visuals/02-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

Reference existing files:
@index.html
@css/style.css
@js/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gallery HTML structure with polaroid figures and lightbox dialog</name>
  <files>index.html</files>
  <action>
Update index.html to add the photo gallery inside main-content and the lightbox dialog.

**In the head section**, add Google Fonts link for Caveat (handwritten font):
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Caveat:wght@400;600&display=swap" rel="stylesheet">
```

**Inside main-content**, add a gallery section with 5 polaroid placeholder images:
```html
<section class="gallery" aria-label="Photo gallery">
  <figure class="polaroid" style="--i: 0; --rotation: -3deg;">
    <img src="images/photos/photo-1.jpg" alt="Photo 1" loading="lazy">
    <figcaption>Caption for photo 1</figcaption>
  </figure>
  <figure class="polaroid" style="--i: 1; --rotation: 4deg;">
    <img src="images/photos/photo-2.jpg" alt="Photo 2" loading="lazy">
    <figcaption>Caption for photo 2</figcaption>
  </figure>
  <figure class="polaroid" style="--i: 2; --rotation: -2deg;">
    <img src="images/photos/photo-3.jpg" alt="Photo 3" loading="lazy">
    <figcaption>Caption for photo 3</figcaption>
  </figure>
  <figure class="polaroid" style="--i: 3; --rotation: 5deg;">
    <img src="images/photos/photo-4.jpg" alt="Photo 4" loading="lazy">
    <figcaption>Caption for photo 4</figcaption>
  </figure>
  <figure class="polaroid" style="--i: 4; --rotation: -4deg;">
    <img src="images/photos/photo-5.jpg" alt="Photo 5" loading="lazy">
    <figcaption>Caption for photo 5</figcaption>
  </figure>
</section>
```

**After main-content closing tag**, add the lightbox dialog:
```html
<!-- Lightbox Dialog -->
<dialog class="lightbox" aria-label="Photo viewer">
  <form method="dialog">
    <button type="submit" class="lightbox-close" aria-label="Close">
      <span aria-hidden="true">&times;</span>
    </button>
  </form>
  <img src="" alt="" class="lightbox-image">
  <p class="lightbox-caption"></p>
</dialog>
```

**Add gallery.js script import** after hearts.js, before main.js:
```html
<script type="module" src="js/gallery.js"></script>
```

Use CSS custom properties --i for stagger delay index and --rotation for scatter angle.
  </action>
  <verify>index.html validates, Google Fonts link present, gallery section has 5 polaroid figures, dialog element present, gallery.js script imported</verify>
  <done>HTML has gallery section with 5 polaroid figures, lightbox dialog, Google Fonts link, and gallery.js import</done>
</task>

<task type="auto">
  <name>Task 2: Create polaroid and lightbox CSS with scatter and deal animations</name>
  <files>css/style.css</files>
  <action>
Add CSS for polaroid gallery and lightbox dialog at the end of style.css.

**CSS custom property for handwritten font:**
```css
--font-handwritten: 'Caveat', cursive;
```

**Gallery container:**
```css
.gallery {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 1rem;
  padding: 2rem 1rem;
  position: relative;
  z-index: 2;
}
```

**Polaroid styling:**
- White background, padding: 0.75rem sides, 2.5rem bottom (for caption space)
- Box-shadow for depth (subtle, realistic)
- transform: rotate(var(--rotation)) for scattered angles
- Initial opacity: 0 for deal animation
- Cursor: pointer
- Transition on transform and box-shadow for hover

**Polaroid image:**
- width: 150px, height: 150px, object-fit: cover on mobile
- width: 180px, height: 180px on tablet (600px+)

**Polaroid caption:**
- font-family: var(--font-handwritten)
- font-size: 1.1rem, text-align: center, color: #333

**Hover lift effect:**
```css
.polaroid:hover,
.polaroid:focus {
  transform: scale(1.05) rotate(0deg);
  box-shadow: 0 8px 16px rgba(0,0,0,0.15), 0 16px 32px rgba(0,0,0,0.1);
  z-index: 10;
}
```

**Deal animation (staggered):**
```css
.gallery.revealed .polaroid {
  animation: deal-polaroid 0.5s ease-out forwards;
  animation-delay: calc(var(--i) * 0.7s);
}

@keyframes deal-polaroid {
  from {
    opacity: 0;
    transform: translateY(-30px) rotate(0deg) scale(0.9);
  }
  to {
    opacity: 1;
    transform: translateY(0) rotate(var(--rotation));
  }
}
```
Note: Animation only plays when .revealed class is added to gallery (by JS on app:started).

**Lightbox dialog:**
- border: none, border-radius: 8px, padding: 0
- max-width: 90vw, max-height: 90vh
- background: white, box-shadow for depth
- ::backdrop with rgba(0,0,0,0.8)

**Lightbox image:**
- max-width: 100%, max-height: 80vh, object-fit: contain

**Lightbox close button:**
- position: absolute, top-right corner
- background: rgba(0,0,0,0.5), color: white, border-radius: 50%
- 2rem x 2rem, font-size 1.5rem

**Lightbox caption:**
- font-family: var(--font-handwritten), text-align: center, padding

**Lightbox animation:**
```css
@media (prefers-reduced-motion: no-preference) {
  .lightbox[open] {
    animation: lightbox-show 0.25s ease-out;
  }
  @keyframes lightbox-show {
    from { opacity: 0; transform: scale(0.95); }
    to { opacity: 1; transform: scale(1); }
  }
}
```

**Reduced motion for polaroids:**
```css
@media (prefers-reduced-motion: reduce) {
  .gallery.revealed .polaroid {
    animation: none;
    opacity: 1;
  }
}
```

**Prevent body scroll when lightbox open:**
```css
body:has(.lightbox[open]) {
  overflow: hidden;
}
```
  </action>
  <verify>Browser: Polaroids render with white borders, captions show handwritten font, hover shows lift effect, lightbox dialog styles correctly</verify>
  <done>CSS has polaroid styling with scatter rotations, deal animation, hover lift, and complete lightbox dialog styling</done>
</task>

<task type="auto">
  <name>Task 3: Create gallery.js module for reveal animation and lightbox interaction</name>
  <files>js/gallery.js</files>
  <action>
Create js/gallery.js as an ES module that handles:
1. Adding .revealed class to gallery on app:started event
2. Lightbox open/close functionality

```javascript
/**
 * Gallery Module
 * Handles polaroid reveal animation and lightbox interaction
 */

const lightbox = document.querySelector('.lightbox');
const lightboxImg = lightbox?.querySelector('.lightbox-image');
const lightboxCaption = lightbox?.querySelector('.lightbox-caption');
const gallery = document.querySelector('.gallery');

/**
 * Open lightbox with photo
 */
function openLightbox(src, alt, caption) {
  if (!lightbox || !lightboxImg) return;

  lightboxImg.src = src;
  lightboxImg.alt = alt;
  lightboxCaption.textContent = caption || '';
  lightbox.showModal();
}

/**
 * Initialize gallery interactions
 */
function initGallery() {
  // Lightbox: close on backdrop click
  lightbox?.addEventListener('click', (e) => {
    if (e.target === lightbox) {
      lightbox.close();
    }
  });

  // Polaroid click handlers
  document.querySelectorAll('.polaroid').forEach(polaroid => {
    polaroid.addEventListener('click', () => {
      const img = polaroid.querySelector('img');
      const caption = polaroid.querySelector('figcaption')?.textContent;
      if (img) {
        openLightbox(img.src, img.alt, caption);
      }
    });

    // Keyboard accessibility
    polaroid.setAttribute('tabindex', '0');
    polaroid.setAttribute('role', 'button');
    polaroid.addEventListener('keydown', (e) => {
      if (e.key === 'Enter' || e.key === ' ') {
        e.preventDefault();
        polaroid.click();
      }
    });
  });
}

/**
 * Trigger reveal animation
 */
function revealGallery() {
  if (gallery) {
    gallery.classList.add('revealed');
  }
}

// Initialize on DOM ready
if (document.readyState === 'loading') {
  document.addEventListener('DOMContentLoaded', initGallery);
} else {
  initGallery();
}

// Reveal gallery when app starts (listen for custom event from main.js)
document.addEventListener('app:started', revealGallery);
```

**Key points:**
- Uses native dialog showModal() for accessibility
- Backdrop click closes lightbox
- Escape key handled automatically by dialog element
- Keyboard accessibility with tabindex and role="button"
- Listens for app:started event to trigger reveal animation
- Gallery stays hidden (opacity:0) until revealed class is added
  </action>
  <verify>`node --check js/gallery.js` passes. Browser: Click polaroid opens lightbox, click backdrop closes, Escape closes, polaroids animate in after start</verify>
  <done>gallery.js initializes lightbox, handles polaroid clicks, and triggers reveal animation on app:started event</done>
</task>

<task type="auto">
  <name>Task 4: Create images directory with placeholder setup</name>
  <files>images/photos/.gitkeep</files>
  <action>
Create the images/photos directory structure for the user's photos.

1. Create directories: `mkdir -p images/photos`
2. Create .gitkeep file: `touch images/photos/.gitkeep`
3. Create placeholder images (simple colored squares) for testing:

Use a simple data URL or create minimal placeholder files. For testing purposes, you can either:
- Create simple placeholder images using an online placeholder service in the HTML (update img src to use https://placehold.co/300x300/ff69b4/ffffff?text=Photo+1 etc.)
- OR add a comment in index.html noting user should add their photos

**Recommended approach:** Update the img src attributes in index.html to use placeholder URLs for development:
```html
src="https://placehold.co/300x300/ff69b4/ffffff?text=Photo+1"
```

This allows immediate visual testing. User will replace with actual photos later.

Add a comment in HTML:
```html
<!-- TODO: Replace placeholder URLs with actual photos in images/photos/ -->
```
  </action>
  <verify>images/photos directory exists, .gitkeep present, placeholder images display in browser</verify>
  <done>images/photos directory created with .gitkeep, placeholder images configured for development testing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete polaroid photo gallery with:
    - 5 polaroid-style photos with handwritten captions
    - Scattered layout at varied angles
    - Staggered "dealt" reveal animation after click-to-start
    - Lightbox that opens on click with photo and caption
    - Hover lift effect on polaroids
  </what-built>
  <how-to-verify>
    1. Open index.html in browser
    2. Verify hearts are floating (from Plan 01 if executed)
    3. Click "Click to Open Your Valentine" button
    4. Watch polaroids animate in one-by-one (should take ~3.5 seconds for all 5)
    5. Hover over a polaroid - should lift slightly with shadow
    6. Click a polaroid - lightbox should open with larger image and caption
    7. Click outside the lightbox (on dark backdrop) - should close
    8. Open another polaroid, press Escape - should close
    9. Test on mobile viewport (Chrome DevTools device mode)
    10. Check prefers-reduced-motion (DevTools > Rendering > Emulate CSS media feature)
  </how-to-verify>
  <resume-signal>Type "approved" if gallery works correctly, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Page loads with gallery hidden (opacity: 0) on main content
2. Click start button - polaroids animate in sequentially
3. Each polaroid has unique rotation angle
4. Hover shows lift effect
5. Click polaroid opens native dialog lightbox
6. Lightbox shows larger image + caption
7. Click backdrop or press Escape closes lightbox
8. Keyboard: Tab to polaroid, Enter opens lightbox
9. Mobile: Touch interactions work, images sized appropriately
10. Reduced motion: Polaroids appear without animation
</verification>

<success_criteria>
- 5 polaroid-style photos with white borders and handwritten captions
- Scattered angles (-4deg to +5deg) for natural look
- Sequential reveal animation (0.7s stagger) after click-to-start
- Lightbox using native dialog with showModal()
- Backdrop click and Escape close lightbox
- Hover lift effect on polaroids
- Keyboard accessible (tabindex, role="button")
- Mobile responsive (smaller images on mobile)
- Reduced motion respected
</success_criteria>

<output>
After completion, create `.planning/phases/02-visuals/02-02-SUMMARY.md`
</output>
